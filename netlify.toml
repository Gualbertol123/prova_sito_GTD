# Netlify build configuration for the TEAM GTD React app.
# The frontend lives in web/ and talks directly to Supabase — there is no
# server to deploy here.

[build]
  command = "npm --prefix web install && npm --prefix web run build"
  publish = "web/dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: serve index.html for any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Hashed build assets (including the big, lazily-loaded SuperDoc editor chunk)
# never change content under the same filename, so they can be cached for good.
# This is what makes the editor a one-time download per version: an ordinary
# app deploy leaves these entries valid, and a SuperDoc upgrade only
# invalidates SuperDoc's own chunk.
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Never let the browser cache the HTML shell — everything is remote/live.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-store"

# Keep the whole site out of search engines.
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"
